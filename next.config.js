/** @type {import('next').NextConfig} */
const nextConfig = {
    // expiremental:{
    //     appDir:true,
    // },
    images:{
        domains:['image.tmdb.org'],
    },
}

module.exports = nextConfig
